
# used for dict names that stores config items
app_config = 'app'
hydra_config = 'hydra'

hydra_run_out="hydra.run.out"

# boot strap defaults
app_config_default = {
  'root_dir':'lightning-pose',
  'config_dir':'scripts/configs',
  'config_name':'config',
}
